version: "3.8"

services:
  catalogdb:
    image: mongo

  basketdb:
    image: redis:alpine

  discountdb:
    image: postgres

  pgadmin:
    image: dpage/pgadmin4  

  portainer:
    image: portainer/portainer-ce

  

volumes:
  mongo_data:
  portainer_data:
  postgres_data:
  pgadmin_data: